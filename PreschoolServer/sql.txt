CREATE TABLE users(
	user_id BIGSERIAL NOT NULL PRIMARY KEY,
	user_name TEXT,
	user_role INT,
	salt CHAR(64),
    user_pwd bytea,
	user_login TEXT,
    UNIQUE (user_login)
);


-------------------------------------------------